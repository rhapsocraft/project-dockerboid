services:
  ProjectZomboidDedicatedServer:
    # Make sure we enforce a specific version
    image: rhapsocraft/project-zomboid-dedicated-server:42.16.1-unstable
    restart: unless-stopped 
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Reads STEAMAPPBRANCH from your .env file to set the build version (ARG).
        - STEAMAPPBRANCH=${STEAMAPPBRANCH}
        - PUID=${PUID}
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 8.8.4.4
      - 1.1.1.1
    ports:
      - "16261:16261/udp"
      - "16262:16262/udp"
      - "27015:27015/tcp"
      - "8766:8766/udp"
      - "8767:8767/udp"
    volumes:
      # Server data
      - ./data:/home/steam/Zomboid
      # Setup file
      - ./config/setup.yml:/server/setup.yml
      # Sandbox Config
      - ./config/server/sandbox.lua:/home/steam/Zomboid/Server/${SERVERNAME}_SandboxVars.lua
      # Workshop mods folder
      - ./workshop-mods:/home/steam/pz-dedicated/steamapps/workshop
      # Reflection Enabler - doesn't appear to be working?
      - ./workshop-mods/content/108600/3682136459/mods/reflection-enabler/zombie:/home/steam/pz-dedicated/zombie
